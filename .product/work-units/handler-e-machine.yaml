id: handler-e-machine
schema: 'Rust module: machine catalog views for loom-graph — states, transitions, gates, focus post-conditions, keyboard bindings per context'
prompt: 'Implement the e-machine slice in crates/loom-graph: typed views over loom:g/machines per pat-typed-row-view — projection layer, overlay layer with gates, transitions (event, from/to, motion ref, keyboard binding per keyboarded context, focus post-conditions). SHACL shapes under shapes/ enforce machine well-formedness on load: overlay transitions gated on declared projection states, no pointer-only transitions (2.1.1 structural), transient-surface transitions declare focus post-conditions, every transition names a motion catalog entry or explicit none (fail-closed per pat-load-gate). These views feed the runtime interpreter, the machine-equivalence oracle, and the gallery''s generated state-drivers — one source, no disagreement.'
model: code
context:
  derived_from:
  - domain:e-machine
  frozen: true
  hash: sha256:b429d11dc82ee39b7cd060136bf057be8e19e0afc65bc3c59c4243ce69dffd06
produces:
  artifact: 'Rust module: machine typed views + well-formedness shapes wiring'
  path: crates/loom-graph/src/machines.rs
trace:
  what: e-machine
  behaviour: loaded via cmd-load-bundle; interpreted by e-component-instance's runtime; judged by the §7.4 oracle
  why:
  - d-machines-as-data
  - p-machine-from-graph
  - p-fail-closed-load
  - pat-typed-row-view
  - pat-load-gate
  structure: loom-graph layer of loom-app
