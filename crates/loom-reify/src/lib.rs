//! loom-reify — the reification seam (d-canonical-contracts).
//!
//! Speaks only the pinned canonical contracts; the run is recorded to the
//! run graph as verdicts are produced (pat-run-recorder).

pub mod run;
